<span class="inline-flex items-center gap-1 px-2 py-0.5 rounded text-xs font-medium {{ $colorClasses() }}">
    <span>{{ __('sla.types.' . $type) }}</span>
    <span>·</span>
    <span>{{ __('sla.status.' . $status) }}</span>
</span>
